% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_wide_dataset.R
\name{convert_wide_to_hourly}
\alias{convert_wide_to_hourly}
\title{Aggregate a wide dataset into fixed-width time windows}
\usage{
convert_wide_to_hourly(
  wide_df,
  aggregation_config,
  id_name = "hospitalization_id",
  hourly_window = 1,
  fill_gaps = FALSE,
  memory_limit = "4GB",
  temp_directory = NULL,
  batch_size = NULL,
  timezone = "UTC"
)
}
\arguments{
\item{wide_df}{A wide dataset from \code{\link[=create_wide_dataset]{create_wide_dataset()}}.}

\item{aggregation_config}{Named list mapping an aggregation method to the columns
it applies to. Supported methods: \code{max}, \code{mean}, \code{min}, \code{median}, \code{first},
\code{last}, \code{boolean}, \code{one_hot_encode}. Columns not listed are carried through with
\code{first} and a \verb{_c} suffix.}

\item{id_name}{Grouping column, e.g. \code{"hospitalization_id"} or \code{"encounter_block"}.}

\item{hourly_window}{Window width in hours, an integer between 1 and 72.}

\item{fill_gaps}{When \code{TRUE}, emit a row for every window between each group's
first and last window, filling gaps with \code{NA}.}

\item{memory_limit}{DuckDB memory limit.}

\item{temp_directory}{Directory for DuckDB spill files.}

\item{batch_size}{Number of groups per batch. \code{NULL} auto-selects; \code{0} disables.}

\item{timezone}{DuckDB session timezone.}
}
\value{
A tibble with \code{window_number}, \code{window_start_dttm}, \code{window_end_dttm},
\code{patient_id}, \code{day_number} and one column per configured aggregate.
}
\description{
Windows are event-based: window 0 starts at each group's first event and each
window spans \code{hourly_window} hours. Port of
\code{clifpy.utils.wide_dataset.convert_wide_to_hourly}.
}
\examples{
\dontrun{
hourly <- convert_wide_to_hourly(
  wide_dataset,
  aggregation_config = list(
    max = c("sbp"), mean = c("heart_rate"), boolean = c("norepinephrine")
  )
)
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_unit_converter.R
\name{convert_dose_units_by_med_category}
\alias{convert_dose_units_by_med_category}
\title{Convert medication dose units to preferred units by medication category}
\usage{
convert_dose_units_by_med_category(
  med_df,
  vitals_df = NULL,
  preferred_units = NULL,
  show_intermediate = FALSE,
  override = FALSE
)
}
\arguments{
\item{med_df}{Medication data frame with \code{med_category}, \code{med_dose} and
\code{med_dose_unit} columns (and \code{weight_kg}, unless \code{vitals_df} is supplied).}

\item{vitals_df}{Optional vitals data frame used to derive \code{weight_kg}
when it is missing from \code{med_df} (see \code{\link[=find_most_recent_weight]{find_most_recent_weight()}}).}

\item{preferred_units}{Named list or character vector mapping medication
categories to preferred units, e.g.
\code{list(propofol = "mcg/kg/min", fentanyl = "mcg/hr", insulin = "u/hr")}.
Categories without an entry fall back to their base units.}

\item{show_intermediate}{If \code{TRUE}, keeps all intermediate calculation
columns (multipliers and QA columns) in the output.}

\item{override}{If \code{TRUE}, warns instead of erroring on unacceptable
preferred units or preferred-unit categories missing from \code{med_df}.}
}
\value{
A named list (clifpy returns a tuple) with elements:
\describe{
\item{converted}{tibble of \code{med_df} with added columns including
\verb{_clean_unit}, \verb{_unit_class}, \verb{_convert_status}, \code{med_dose_converted}
and \code{med_dose_unit_converted} (plus intermediate multiplier and QA
columns when \code{show_intermediate = TRUE}).}
\item{counts}{tibble of conversion counts grouped by \code{med_category},
original/clean/base/preferred units and conversion status.}
}
}
\description{
Two-step conversion, ported from clifpy's
\code{convert_dose_units_by_med_category}:
}
\details{
\enumerate{
\item Standardize all dose units to a base set of standard units
(mcg/min, ml/min, u/min for rates; mcg, ml, u for amounts).
\item Convert from base units to each medication category's preferred unit,
maintaining unit class consistency (rates stay rates, amounts stay
amounts) and using patient weight for weight-based dosing.
}
}
\examples{
\dontrun{
conversion_result <- convert_dose_units_by_med_category(
  medication_data,
  vitals_df = vitals_data,
  preferred_units = list(
    propofol = "mcg/kg/min",
    fentanyl = "mcg/hr",
    insulin = "u/hr"
  )
)
conversion_result$converted
conversion_result$counts
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_stitching_encounters.R
\name{stitch_encounters}
\alias{stitch_encounters}
\title{Stitch together related hospital encounters}
\usage{
stitch_encounters(hospitalization, adt, time_interval = 6)
}
\arguments{
\item{hospitalization}{A \code{Hospitalization} table object or data frame with
required columns \code{patient_id}, \code{hospitalization_id}, \code{admission_dttm},
\code{discharge_dttm}, \code{age_at_admission}, \code{admission_type_category} and
\code{discharge_category}.}

\item{adt}{An \code{Adt} table object or data frame with required columns
\code{hospitalization_id}, \code{in_dttm}, \code{out_dttm}, \code{location_category} and
\code{hospital_id}.}

\item{time_interval}{Integer, default \code{6}. Number of hours between discharge
and the next admission for the encounters to be considered linked.}
}
\value{
A named list with three tibbles (clifpy returns the same three
objects as a tuple):
\describe{
\item{hospitalization}{The input hospitalization data with an added
\code{encounter_block} column.}
\item{adt}{The input ADT data with an added \code{encounter_block} column.}
\item{encounter_mapping}{Mapping of \code{hospitalization_id} to
\code{encounter_block}.}
}
}
\description{
Identifies and groups hospitalizations that occur within a specified time
window of each other (default 6 hours), treating them as a single continuous
encounter. If a patient is readmitted within \code{time_interval} hours of a
discharge, the two hospitalizations receive the same \code{encounter_block}.
Chains of linked hospitalizations share one block, and blocks are numbered
by the row index (within the patient/admission-time sort) of the last
hospitalization in each chain, matching clifpy exactly.
}
\examples{
\dontrun{
stitched <- stitch_encounters(hospitalization_data, adt_data, time_interval = 12)
stitched$hospitalization
stitched$adt
stitched$encounter_mapping
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_wide_dataset.R
\name{clif-wide-dataset}
\alias{clif-wide-dataset}
\title{Wide dataset creation utilities}
\description{
Port of \code{clifpy/utils/wide_dataset.py}. Wide datasets are built by a DuckDB SQL
pipeline: narrow tables are pivoted with DuckDB's \code{PIVOT} statement, keyed on a
\code{hospitalization_id}/minute "combo id", and joined onto the union of every event
timestamp. The same SQL is issued here through R's \code{duckdb} package so the
resulting columns, column order and values match the Python implementation.
}
\details{
Two behaviours from clifpy are reproduced deliberately even though they look like
oversights, because parity is the requirement:
\itemize{
\item \code{save_to_data_location} and \code{return_dataframe} only take effect in batch mode
(\code{batch_size > 0} and more hospitalizations than \code{batch_size}); single-pass
processing always returns the dataset and never writes it.
\item \code{category_filters} means different things per table type — category \emph{values} for
pivot tables, column \emph{names} for already-wide tables.
}

Timezone handling: clifpy holds timezone-aware pandas columns and lets DuckDB's
session timezone decide the wall clock used by \code{strftime}. R's \code{duckdb} registers
\code{POSIXct} as a naive \code{TIMESTAMP} holding the true UTC instant, so the instant
survives registration unshifted (distinct instants stay distinct even across a DST
fall-back) and the site-local wall clock needed for combo ids is reconstructed in
SQL with a double \verb{AT TIME ZONE}. Returned datetimes are re-labelled to the site
timezone without shifting the instant. See the timezone helpers below.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_migrate_versions.R
\name{CrosswalkMigrationRunner}
\alias{CrosswalkMigrationRunner}
\title{Migrate a directory of CLIF 2.1 tables to 3.0 value conventions}
\description{
Migrate a directory of CLIF 2.1 tables to 3.0 value conventions

Migrate a directory of CLIF 2.1 tables to 3.0 value conventions
}
\details{
R6 port of clifpy's \code{CrosswalkMigrationRunner}. Migrates every beta table in a
data folder by calling \code{\link[=crosswalk_file_2_1_to_3_0]{crosswalk_file_2_1_to_3_0()}} and verifies row count,
column set, and distinct ID counts before and after each conversion, reporting
timezone changes via \code{\link[=tz_status]{tz_status()}}.
}
\section{Public fields}{
\if{html}{\out{<div class="r6-fields">}}
\describe{
\item{\code{config_path}}{Optional path to a JSON or YAML config file.}

\item{\code{data_dir}}{Directory holding the CLIF 2.1 input tables.}

\item{\code{output_dir}}{Directory the migrated CLIF 3.0 tables are written to.}

\item{\code{filetype}}{Data file type. Only \code{"parquet"} is verified.}

\item{\code{log_dir}}{Directory the run log is written to.}

\item{\code{log_path}}{Path of the timestamped run log file.}
}
\if{html}{\out{</div>}}
}
\section{Methods}{
\subsection{Public methods}{
\itemize{
\item \href{#method-CrosswalkMigrationRunner-new}{\code{CrosswalkMigrationRunner$new()}}
\item \href{#method-CrosswalkMigrationRunner-audit}{\code{CrosswalkMigrationRunner$audit()}}
\item \href{#method-CrosswalkMigrationRunner-run}{\code{CrosswalkMigrationRunner$run()}}
\item \href{#method-CrosswalkMigrationRunner-clone}{\code{CrosswalkMigrationRunner$clone()}}
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-CrosswalkMigrationRunner-new"></a>}}
\if{latex}{\out{\hypertarget{method-CrosswalkMigrationRunner-new}{}}}
\subsection{Method \code{new()}}{
Construct a migration runner.

Resolves the data and output directories either from a config file (via a
\link{ClifOrchestrator}, which also carries the filetype) or from explicit
\code{data_dir} / \code{output_dir}, then sets up a timestamped run log.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{CrosswalkMigrationRunner$new(
  config_path = NULL,
  data_dir = NULL,
  output_dir = NULL,
  filetype = "parquet",
  log_dir = NULL
)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{config_path}}{Optional path to a JSON or YAML config file. When given
and both directories are not, the directories and filetype come from the
config.}

\item{\code{data_dir}}{Directory containing the CLIF 2.1 input tables.}

\item{\code{output_dir}}{Directory to write the migrated CLIF 3.0 tables into.}

\item{\code{filetype}}{Data file type, defaults to \code{"parquet"}.}

\item{\code{log_dir}}{Directory for the run log. Defaults to \verb{<output_dir>/logs}.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
A new \code{CrosswalkMigrationRunner}.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-CrosswalkMigrationRunner-audit"></a>}}
\if{latex}{\out{\hypertarget{method-CrosswalkMigrationRunner-audit}{}}}
\subsection{Method \code{audit()}}{
Bucket every file in the data directory.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{CrosswalkMigrationRunner$audit()}\if{html}{\out{</div>}}
}

\subsection{Returns}{
A named list with \code{all_files} (named list of stem to path),
\code{beta} (beta tables present), \code{non_beta} (present tables that are not
beta; logged but never written) and \code{missing} (beta tables absent from
the folder).
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-CrosswalkMigrationRunner-run"></a>}}
\if{latex}{\out{\hypertarget{method-CrosswalkMigrationRunner-run}{}}}
\subsection{Method \code{run()}}{
Run the migration.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{CrosswalkMigrationRunner$run(dry_run = FALSE)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{dry_run}}{When \code{TRUE}, audit only: report what would change and write
nothing.}
}
\if{html}{\out{</div>}}
}
\subsection{Returns}{
\code{TRUE} on success (no failures and no integrity mismatches).
\code{is_complete = FALSE} is not a failure: it means some values need manual
mapping and are reported, not lost.
}
}
\if{html}{\out{<hr>}}
\if{html}{\out{<a id="method-CrosswalkMigrationRunner-clone"></a>}}
\if{latex}{\out{\hypertarget{method-CrosswalkMigrationRunner-clone}{}}}
\subsection{Method \code{clone()}}{
The objects of this class are cloneable with this method.
\subsection{Usage}{
\if{html}{\out{<div class="r">}}\preformatted{CrosswalkMigrationRunner$clone(deep = FALSE)}\if{html}{\out{</div>}}
}

\subsection{Arguments}{
\if{html}{\out{<div class="arguments">}}
\describe{
\item{\code{deep}}{Whether to make a deep clone.}
}
\if{html}{\out{</div>}}
}
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_logging_config.R
\name{setup_logging}
\alias{setup_logging}
\title{Configure logging for clifR}
\usage{
setup_logging(
  output_directory = NULL,
  level = "info",
  console_output = TRUE,
  separate_error_log = TRUE
)
}
\arguments{
\item{output_directory}{Base output directory. Logs are written to
\verb{<output_directory>/logs/}. When \code{NULL}, uses \verb{<working directory>/output}.}

\item{level}{Minimum severity to record: \code{"debug"}, \code{"info"}, \code{"warning"},
\code{"error"} or \code{"critical"}. A numeric Python-style severity is also accepted.}

\item{console_output}{Whether to also print records to the console.}

\item{separate_error_log}{Whether to write a second file containing warnings
and errors only.}
}
\value{
The root clifR logger, invisibly. See \code{\link[=get_logger]{get_logger()}}.
}
\description{
Creates \verb{<output_directory>/logs/} and directs log records to \code{clifr_all.log},
\code{clifr_errors.log} and the console. Calling this again reconfigures logging
with the new parameters, so it is safe to call repeatedly — for example when
an orchestrator changes its output directory.
}
\examples{
\dontrun{
logger <- setup_logging(output_directory = "./output")
logger$info("Loading data...")
}
}

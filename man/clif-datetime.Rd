% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_datetime.R
\name{clif-datetime}
\alias{clif-datetime}
\alias{standardize_datetime_columns_polars}
\alias{ensure_datetime_precision_match_polars}
\alias{convert_datetime_columns_to_site_tz_polars}
\title{Datetime standardization utilities}
\usage{
standardize_datetime_columns_polars(
  df,
  target_timezone,
  target_time_unit = "ns",
  ambiguous = "earliest",
  non_existent = "null",
  datetime_columns = NULL
)

ensure_datetime_precision_match_polars(
  df1,
  df2,
  df1_datetime_col,
  df2_datetime_col,
  target_timezone,
  target_time_unit = "ns"
)

convert_datetime_columns_to_site_tz_polars(df, site_tz_str, verbose = TRUE)
}
\arguments{
\item{df}{A data frame.}

\item{target_timezone}{Olson timezone name.}

\item{target_time_unit}{Accepted for clifpy compatibility; ignored in R.}

\item{ambiguous}{Accepted for clifpy compatibility; R's \code{POSIXct} resolves DST
ambiguity by its own rules and does not take this option.}

\item{non_existent}{Accepted for clifpy compatibility; ignored in R.}

\item{datetime_columns}{Optional character vector of columns to convert. When
\code{NULL}, all datetime columns are detected automatically.}

\item{df1}{First data frame.}

\item{df2}{Second data frame.}

\item{df1_datetime_col}{Datetime column name in \code{df1}.}

\item{df2_datetime_col}{Datetime column name in \code{df2}.}

\item{site_tz_str}{Olson timezone name.}

\item{verbose}{Whether to emit a conversion summary.}
}
\value{
The data frame with standardized datetime columns.

A named list with elements \code{df1} and \code{df2}.

The data frame with datetime columns in \code{site_tz_str}.
}
\description{
R ports of clifpy's \code{datetime_polars.py}. In clifpy these exist because Polars
tracks both a timezone and a time unit (ms/us/ns) per datetime column, so joins
can fail on a precision mismatch. R's \code{POSIXct} carries a single \code{tzone} attribute
and always stores seconds as a double, so there is no time-unit axis to reconcile;
the \code{target_time_unit} argument is accepted for signature compatibility but has no
effect. The timezone behaviour is ported faithfully.
}
\details{
Because these mirror clifpy's Polars entry points, they are also exported under
the \verb{*_polars} names clifpy re-exports at its package root, so code translated
from Python keeps working.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_outlier_handler.R
\name{apply_outlier_handling}
\alias{apply_outlier_handling}
\title{Apply outlier handling to a CLIF table object}
\usage{
apply_outlier_handling(table_obj, outlier_config_path = NULL)
}
\arguments{
\item{table_obj}{A clifR table object (a \link{BaseTable} subclass) with \verb{$df} and
\verb{$table_name}.}

\item{outlier_config_path}{Optional path to a custom outlier configuration
YAML. When \code{NULL}, the packaged CLIF standard configuration is used.}
}
\value{
The same table object, invisibly, with out-of-range values in \verb{$df}
set to \code{NA}.
}
\description{
Sets every configured numeric value that falls outside its acceptable range to
\code{NA}.
}
\details{
\strong{The input object is modified.} clifR table objects are R6 objects with
reference semantics, so \code{table_obj$df} is replaced in place exactly as clifpy
replaces \code{table_obj.df}; the returned value is the same object, not a copy.
Take a copy of \code{table_obj$df} beforehand if you need the original values.
}
\examples{
\dontrun{
vitals <- clif_table_from_file("vitals", "data/clif", "parquet", clif_version = "3.0")
apply_outlier_handling(vitals)
}
}

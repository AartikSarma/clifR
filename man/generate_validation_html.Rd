% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_report_generator.R
\name{generate_validation_html}
\alias{generate_validation_html}
\alias{generate_validation_pdf}
\title{Generate an HTML DQA report for a single table}
\usage{
generate_validation_html(
  validation_data,
  table_name,
  output_path,
  site_name = NULL,
  feedback = NULL
)

generate_validation_pdf(
  validation_data,
  table_name,
  output_path,
  site_name = NULL,
  feedback = NULL
)
}
\arguments{
\item{validation_data}{The list \code{\link[=run_full_dqa]{run_full_dqa()}} returns, optionally carrying
\code{table_stats} and \code{total_rows} for the data-profile section.}

\item{table_name}{Name of the table.}

\item{output_path}{Path where the text file is written.}

\item{site_name}{Optional site/hospital label.}

\item{feedback}{Optional feedback list with \code{user_decisions}.}
}
\value{
\code{output_path}, invisibly.
}
\description{
clifR's portable stand-in for clifpy's \code{generate_validation_pdf}: a
self-contained HTML document (no external assets) with the same DQA summary,
data profile and per-category issue detail sections. Temporal-consistency
rows render their per-year presence as an inline SVG spark bar.

\code{generate_validation_pdf()} is a thin alias kept for clifpy call-site
compatibility. R has no reportlab equivalent, so it forwards to
\code{\link[=generate_validation_html]{generate_validation_html()}} and emits a message noting the substitution.
The returned path is the HTML file.
}
\examples{
\dontrun{
generate_validation_html(validation_data, "patient", tempfile(fileext = ".html"))
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_crosswalk.R
\name{crosswalk_file_2_1_to_3_0}
\alias{crosswalk_file_2_1_to_3_0}
\title{Crosswalk a CLIF 2.1 table file to 3.0, writing the result}
\usage{
crosswalk_file_2_1_to_3_0(
  input_path,
  output_path,
  table_name,
  backend = c("duckdb", "memory"),
  chunk_size = 1e+06,
  crosswalk_path = NULL
)
}
\arguments{
\item{input_path}{Source file.}

\item{output_path}{Destination file.}

\item{table_name}{snake_case CLIF table name.}

\item{backend}{\code{"duckdb"} streams the whole transform in SQL, handling
larger-than-RAM inputs; \code{"memory"} reads the file into R and delegates to
\code{\link[=crosswalk_table_2_1_to_3_0]{crosswalk_table_2_1_to_3_0()}}.}

\item{chunk_size}{Accepted for signature parity with clifpy's chunked pandas
backend. Neither R backend needs it: the DuckDB backend streams and the
in-memory backend reads the file whole.}

\item{crosswalk_path}{Optional override path to the crosswalk resource.}
}
\value{
The aggregated change report, as a named list.
}
\description{
For tables too large to hold in memory comfortably. Reads \code{input_path},
converts the standardized columns and writes the converted table to
\code{output_path}. Both paths may be \code{.parquet}/\code{.pq} or \code{.csv}; the format is
inferred from the extension. Returns the same report shape as
\code{\link[=crosswalk_table_2_1_to_3_0]{crosswalk_table_2_1_to_3_0()}}, aggregated over the whole file.
}
\examples{
\dontrun{
report <- crosswalk_file_2_1_to_3_0(
  "clif_labs_21.parquet", "clif_labs_30.parquet", "labs"
)
report$is_complete
}
}

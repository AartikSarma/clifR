% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_validator.R
\name{check_relational_integrity}
\alias{check_relational_integrity}
\title{K.4 Check bidirectional relational integrity between two tables}
\usage{
check_relational_integrity(
  target_df,
  reference_df,
  target_table,
  reference_table,
  key_column
)
}
\arguments{
\item{target_df}{The target table (e.g. labs).}

\item{reference_df}{The reference table (e.g. hospitalization).}

\item{target_table}{Name of the target table.}

\item{reference_table}{Name of the reference table.}

\item{key_column}{The shared key column, e.g. \code{"hospitalization_id"}.}
}
\value{
A \code{dqa_completeness_result} with forward and reverse coverage metrics.
}
\description{
Runs the directional check both ways:
}
\details{
\itemize{
\item \strong{Forward} (reference to target) — what share of reference IDs appear in
the target table? This routinely fails legitimately on inpatient data, so
it is always surfaced as a warning with \code{atomic_count = 0}.
\item \strong{Reverse} (target to reference) — the true foreign-key question. Above
10\% orphans it becomes an error, above 1\% a warning, otherwise it passes
silently.
}
}
\examples{
\dontrun{
check_relational_integrity(labs_data, hosp_data, "labs", "hospitalization",
                           "hospitalization_id")
}
}

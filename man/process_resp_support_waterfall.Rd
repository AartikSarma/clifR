% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_waterfall.R
\name{process_resp_support_waterfall}
\alias{process_resp_support_waterfall}
\title{Clean and waterfall-fill the CLIF respiratory support table}
\usage{
process_resp_support_waterfall(
  resp_support,
  id_col = "hospitalization_id",
  bfill = FALSE,
  verbose = TRUE
)
}
\arguments{
\item{resp_support}{A data frame of raw CLIF respiratory support data, already
in UTC.}

\item{id_col}{Encounter-level identifier column. Defaults to
\code{"hospitalization_id"}.}

\item{bfill}{When \code{TRUE}, numeric setters are back-filled after the
forward-fill; when \code{FALSE} (the default) only forward-fill is applied.}

\item{verbose}{Whether to print progress messages.}
}
\value{
A tibble with the scaffold rows merged in, the hierarchical episode
identifiers added (\code{device_cat_id}, \code{device_id}, \code{mode_cat_id},
\code{mode_name_id}, plus the \code{is_scaffold} flag) and the numeric setters filled,
sorted by \code{id_col} and \code{recorded_dttm}.
}
\description{
Port of \code{clifpy.utils.waterfall.process_resp_support_waterfall}. Timestamps are
expected to already be in UTC; no timezone conversion is performed.
}
\details{
The pipeline runs in four phases:
\enumerate{
\item \strong{Setup} — lowercase the categorical labels, coerce the numeric setters,
rescale FiO2 recorded as percentages, and build an hourly scaffold of
\code{HH:59:59} rows spanning each encounter.
\item \strong{Heuristics} — infer \code{imv} from ventilator modes and from neighbouring
rows with plausible PEEP / rate / tidal volume, infer \code{nippv} from
neighbouring rows with plausible pressures, drop duplicate and empty rows,
and clear \verb{nasal cannula} labels that carry PEEP.
\item \strong{Episode identifiers} — forward-fill device and mode labels and derive the
nested \code{device_cat_id}, \code{device_id}, \code{mode_cat_id} and \code{mode_name_id}
counters.
\item \strong{Waterfall fill} — fill the numeric setters inside each
\verb{(id_col, mode_name_id)} block, restarting at each \verb{trach collar} row, then
forward-fill the tracheostomy flag and tidy up.
}
}
\examples{
\dontrun{
resp_support <- load_data("respiratory_support", "data/clif", "parquet", site_tz = "UTC")
waterfalled <- process_resp_support_waterfall(resp_support)
}
}

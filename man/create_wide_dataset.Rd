% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_wide_dataset.R
\name{create_wide_dataset}
\alias{create_wide_dataset}
\title{Create a wide time-series dataset from CLIF tables}
\usage{
create_wide_dataset(
  clif_instance,
  optional_tables = NULL,
  category_filters = NULL,
  sample = FALSE,
  hospitalization_ids = NULL,
  cohort_df = NULL,
  output_format = "dataframe",
  save_to_data_location = FALSE,
  output_filename = NULL,
  return_dataframe = TRUE,
  base_table_columns = NULL,
  batch_size = 1000,
  memory_limit = NULL,
  threads = NULL,
  show_progress = TRUE
)
}
\arguments{
\item{clif_instance}{A \link{ClifOrchestrator} object (or any list-like object exposing
\code{timezone}, \code{data_directory} and one field per CLIF table, each with a \code{df}).}

\item{optional_tables}{Deprecated. Used only when \code{category_filters} is empty, in
which case each named table is loaded with no category filter.}

\item{category_filters}{Named list mapping table name to a character vector. For
\strong{pivot} tables the values are category values to keep and pivot into columns;
for \strong{wide} tables (e.g. \code{respiratory_support}) they are column names to keep.
The names of this list determine which tables are loaded.}

\item{sample}{When \code{TRUE}, randomly select 20 hospitalizations.}

\item{hospitalization_ids}{Optional character vector of hospitalization IDs.}

\item{cohort_df}{Optional data frame with \code{hospitalization_id}, \code{start_time} and
\code{end_time}; events outside each window are dropped.}

\item{output_format}{\code{"dataframe"}, \code{"csv"} or \code{"parquet"}.}

\item{save_to_data_location}{Whether to write the result into \code{data_directory}.
Only honoured in batch mode, matching clifpy.}

\item{output_filename}{File name stem for the saved file. Defaults to
\code{wide_dataset_YYYYMMDD_HHMMSS}.}

\item{return_dataframe}{Whether to return the dataset when saving. Only honoured
in batch mode, matching clifpy.}

\item{base_table_columns}{Deprecated and unused; base columns are selected
automatically.}

\item{batch_size}{Number of hospitalizations per batch. \code{0} disables batching.}

\item{memory_limit}{Optional DuckDB memory limit, e.g. \code{"8GB"}.}

\item{threads}{Optional DuckDB thread count.}

\item{show_progress}{Whether to report per-batch progress.}
}
\value{
A tibble with one row per hospitalization per event time, or \code{NULL} when
batching is used with \code{return_dataframe = FALSE}.
}
\description{
Joins the base cohort (hospitalization, patient, ADT) to the union of every event
timestamp across the requested tables, pivoting narrow tables into one column per
category. Port of \code{clifpy.utils.wide_dataset.create_wide_dataset}.
}
\examples{
\dontrun{
orchestrator <- ClifOrchestrator$new(
  data_directory = "data/clif", filetype = "parquet", timezone = "UTC"
)
wide_dataset <- create_wide_dataset(
  orchestrator,
  category_filters = list(
    vitals = c("heart_rate", "sbp", "spo2"),
    labs = c("hemoglobin", "creatinine"),
    respiratory_support = c("device_category", "fio2_set")
  )
)
}
}

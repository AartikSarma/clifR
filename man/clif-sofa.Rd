% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_sofa.R
\name{clif-sofa}
\alias{clif-sofa}
\title{SOFA score computation from a wide dataset}
\description{
Port of \code{clifpy/utils/sofa.py}. The Python implementation is a chain of DuckDB
queries, so this port runs the same SQL through R's \code{duckdb} package rather
than reimplementing the logic in dplyr: the aggregation, the \code{CASE} ladders and
the null semantics then behave identically in both languages.
}
\details{
The pipeline is order-sensitive:
\enumerate{
\item outlier clamping (\code{po2_arterial}, \code{fio2_set}, \code{spo2}),
\item PaO2 imputation from SpO2 (Severinghaus, only where \code{spo2 < 97}),
\item extremal (worst) aggregation per ID,
\item component scoring from the aggregated values.
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_validator.R
\name{clif-validator}
\alias{clif-validator}
\title{Data Quality Assessment (DQA) engine}
\description{
Port of \code{clifpy/utils/validator.py} (clifpy 0.5.0). Implements the three
DQA families clifpy defines:
}
\details{
\itemize{
\item \strong{Conformance} — table presence, required columns, dtypes, datetime
format, lab reference units, categorical values, category-group mapping.
\item \strong{Completeness} — missingness, conditional requirements, mCIDE value
coverage, relational integrity.
\item \strong{Plausibility} — chronological order, numeric ranges, field rules,
medication dose units, cross-table temporal bounds, overlapping periods,
category temporal consistency, duplicate composite keys.
}

clifpy ships a Polars and a DuckDB backend that produce identical messages;
only one implementation is ported here. All rule content is read from
\code{validation_rules.yaml} and \code{outlier_config.yaml} via \code{\link[=load_shared_config]{load_shared_config()}}
rather than being restated in R, so the two implementations cannot drift.
}
\section{Case normalization}{

clifpy lowercases column \emph{names} and lowercases + trims every string
\emph{value} before validating, keeping a \verb{__orig_<col>} sidecar so error
payloads still cite what the site actually submitted. \code{\link[=normalize_for_validation]{normalize_for_validation()}}
reproduces that, and every check that compares strings depends on it.
}


% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_unit_converter.R
\name{find_most_recent_weight}
\alias{find_most_recent_weight}
\title{Find the most recent weight for each medication administration}
\usage{
find_most_recent_weight(med_df, vitals_df)
}
\arguments{
\item{med_df}{Medication data frame with \code{hospitalization_id}, \code{admin_dttm}
and \code{med_category} columns.}

\item{vitals_df}{Vitals data frame with \code{hospitalization_id},
\code{recorded_dttm}, \code{vital_category} and \code{vital_value} columns; rows with
\code{vital_category == 'weight_kg'} supply the weights.}
}
\value{
A tibble with all columns of \code{med_df} plus \code{weight_kg} and
\verb{_weight_recorded_dttm}, ordered by \code{hospitalization_id}, \code{admin_dttm},
\code{med_category}.
}
\description{
ASOF-joins the latest \code{weight_kg} vital recorded at or before each
medication \code{admin_dttm}. Port of clifpy's \code{find_most_recent_weight}; the
DuckDB SQL is ported verbatim.
}
\examples{
\dontrun{
medications_with_weight <- find_most_recent_weight(medication_data, vitals_data)
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_crosswalk.R
\name{clif-crosswalk}
\alias{clif-crosswalk}
\title{CLIF 2.1 to 3.0 value crosswalk}
\description{
Port of \code{clifpy/utils/crosswalk.py}. The dominant change from CLIF 2.1 to 3.0
is that the standardized \verb{*_category} / \verb{*_group} / \verb{*_type} column \strong{values}
were lowercased and snake_cased, with a minority of non-derivable renames
(\verb{High Flow NC} -> \code{hfnc}, \code{DNR/DNI} -> \code{dnr_or_dni}, \verb{Psychiatric Hospital}
-> \code{mental_health_hosp}).
}
\details{
Most values are handled by \code{\link[=normalize_category_value]{normalize_category_value()}}, a deterministic
lowercase + snake_case transform; the rest come from the curated resource at
\code{inst/schemas/crosswalks/clif_2.1_to_3.0.yaml}.

The converter is non-mutating: it returns a converted copy of the input plus a
structured report. Values it cannot confidently map — 1-to-many splits such as
\code{albumin} -> \code{albumin_5}/\code{albumin_25}, or anything that does not resolve to a
valid 3.0 permissible value — are left unchanged and surfaced in the report.
}

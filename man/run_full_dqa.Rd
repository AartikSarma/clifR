% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_validator.R
\name{run_full_dqa}
\alias{run_full_dqa}
\title{Run the complete DQA suite on a single table}
\usage{
run_full_dqa(
  df,
  schema = NULL,
  table_name = "",
  tables = NULL,
  error_threshold = 50,
  warning_threshold = 10,
  hosp_years = NULL,
  plausibility_thresholds = NULL,
  clif_version = DEFAULT_CLIF_VERSION
)
}
\arguments{
\item{df}{The data to validate.}

\item{schema}{Parsed table schema. When \code{NULL}, it is loaded from the
packaged schemas using \code{table_name} and \code{clif_version}.}

\item{table_name}{Name of the table. Required.}

\item{tables}{Optional list of objects with \code{table_name} and \code{df} fields.}

\item{error_threshold}{Percent missing above which an error is raised.}

\item{warning_threshold}{Percent missing above which a warning is raised.}

\item{hosp_years}{Optional set of hospitalization years for P.6 context.}

\item{plausibility_thresholds}{Optional named list of per-check threshold overrides.}

\item{clif_version}{CLIF schema version used when \code{schema} is \code{NULL}.}
}
\value{
A named list with \code{table_name}, \code{backend}, \code{conformance},
\code{completeness}, \code{relational} and \code{plausibility} entries.
}
\description{
Orchestrates conformance, completeness and plausibility checks, plus — when
\code{tables} is supplied — auto-detected relational integrity and cross-table
plausibility checks.
}
\examples{
\dontrun{
run_full_dqa(patient_data, table_name = "patient", clif_version = "3.0")
}
}

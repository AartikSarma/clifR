% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_logging_config.R
\name{get_logger}
\alias{get_logger}
\title{Get a logger for a clifR module}
\usage{
get_logger(name)
}
\arguments{
\item{name}{Module name. Prefixed with \code{clifR.} unless it already begins with
\code{clifR}.}
}
\value{
A named list with a \code{name} element and \code{debug}, \code{info}, \code{warning},
\code{error} and \code{critical} functions, each taking a message string.
}
\description{
Returns a logger whose name lives in the \verb{clifR.*} namespace, mirroring
clifpy's \code{get_logger}. The result is a list of message functions, one per
severity, that route through the configuration set by \code{\link[=setup_logging]{setup_logging()}}.
}
\examples{
logger <- get_logger("tables.patient")
logger$name
}

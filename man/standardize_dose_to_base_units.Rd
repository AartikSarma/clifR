% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_unit_converter.R
\name{standardize_dose_to_base_units}
\alias{standardize_dose_to_base_units}
\title{Standardize medication dose units to a base set of standard units}
\usage{
standardize_dose_to_base_units(med_df, vitals_df = NULL)
}
\arguments{
\item{med_df}{Medication data frame with \code{med_dose_unit} and \code{med_dose}
columns (and \code{weight_kg}, unless \code{vitals_df} is supplied).}

\item{vitals_df}{Optional vitals data frame used to derive \code{weight_kg}
when it is missing from \code{med_df}.}
}
\value{
A named list (clifpy returns a tuple of two DuckDB relations) with
elements:
\describe{
\item{base}{tibble of \code{med_df} with added columns \verb{_clean_unit},
\verb{_unit_class} ('rate', 'amount' or 'unrecognized'), \verb{_weighted},
\verb{_amount_multiplier}, \verb{_time_multiplier}, \verb{_weight_multiplier},
\verb{_base_dose} and \verb{_base_unit}.}
\item{counts}{tibble summarizing conversion patterns, grouped by
\code{med_dose_unit}, \verb{_clean_unit}, \verb{_base_unit} and \verb{_unit_class}, with a
\code{count} column.}
}
}
\description{
Complete first-stage standardization pipeline: format cleaning, name
cleaning and conversion to base units (mcg/min, ml/min, u/min for rates;
mcg, ml, u for amounts). Port of clifpy's \code{standardize_dose_to_base_units}.
}
\details{
If \code{med_df} has no \code{weight_kg} column, the most recent weight for each
administration is pulled from \code{vitals_df} via \code{\link[=find_most_recent_weight]{find_most_recent_weight()}}.
Weight-based dosing (\verb{/kg}, \verb{/lb}), time conversions (\verb{/hr} to \verb{/min}),
volume (l to ml), mass (mg, ng, g to mcg) and milli-unit conversions are
handled automatically; unrecognized units are flagged but preserved.
}
\examples{
\dontrun{
standardized <- standardize_dose_to_base_units(medication_data, vitals_data)
standardized$base
standardized$counts
}
}

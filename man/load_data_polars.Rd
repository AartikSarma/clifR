% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_io.R
\name{load_data_polars}
\alias{load_data_polars}
\title{Load a CLIF table (Polars-compatible entry point)}
\usage{
load_data_polars(
  table_name,
  table_path,
  table_format_type,
  sample_size = NULL,
  columns = NULL,
  filters = NULL,
  site_tz = NULL,
  lazy = TRUE,
  verbose = FALSE
)
}
\arguments{
\item{table_name}{snake_case table name, e.g. \code{"labs"}.}

\item{table_path}{Directory containing the data file.}

\item{table_format_type}{Either \code{"csv"} or \code{"parquet"}.}

\item{sample_size}{Optional maximum number of rows to read.}

\item{columns}{Optional character vector of columns to select.}

\item{filters}{Optional named list of equality filters. A length-1 value becomes
\code{column = value}; a longer vector becomes \verb{column IN (...)}.}

\item{site_tz}{Optional Olson timezone to convert \code{dttm} columns into.}

\item{lazy}{Accepted for clifpy compatibility; ignored in R.}

\item{verbose}{Whether to emit loading messages.}
}
\value{
A tibble.
}
\description{
R port of clifpy's \code{load_data_polars}. clifpy ships a Polars-backed loader
alongside its pandas one; clifR has a single DuckDB-backed engine, so this
delegates to \code{\link[=load_data]{load_data()}} and returns a tibble. The \code{lazy} argument is accepted
for signature compatibility and is a no-op — clifR always returns a materialized
tibble.
}

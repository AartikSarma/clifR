% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_migrate_versions.R
\name{clif-migrate-versions}
\alias{clif-migrate-versions}
\title{CLIF 2.1 to 3.0 directory migration runner}
\description{
Port of \code{clifpy/utils/migrate_versions_2_1_to_3.py}. Wraps the already-ported
value crosswalk (\code{\link[=crosswalk_file_2_1_to_3_0]{crosswalk_file_2_1_to_3_0()}}) to migrate an entire site data
folder in one pass:
}
\details{
\itemize{
\item audits every file in the folder (beta / non-beta / missing beta),
\item crosswalks the beta tables (standardized VALUES only; nothing else changes),
\item logs every non-beta file but does \strong{not} process or write it (so the PHI
variant and scratch files are never written automatically),
\item verifies each conversion preserved row count, column set, and distinct ID
counts, reading parquet schema and metadata rather than full data,
\item reports timezone changes; a relabel of tz-aware timestamps to UTC (what the
DuckDB backend does, instants preserved) is \strong{not} treated as a failure,
\item wraps each table in a try/catch so one bad table never aborts the run.
}

The runner assumes parquet input (verification reads parquet metadata).
}

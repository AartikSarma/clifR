% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_crosswalk.R
\name{normalize_category_value}
\alias{normalize_category_value}
\title{Lowercase and snake_case a single category value}
\usage{
normalize_category_value(value)
}
\arguments{
\item{value}{A value or vector of values to normalize.}
}
\value{
The normalized value, same length as \code{value}.
}
\description{
Reproduces the CLIF 3.0 token convention for values that changed by case or
punctuation only. Non-character input is returned unchanged, so numeric flag
columns such as \code{tracheostomy} (0/1) pass through untouched. \code{NA} is preserved.
}
\details{
Rules, applied in order:
\enumerate{
\item Non-character or missing input is returned unchanged.
\item Trim surrounding whitespace, then lowercase.
\item \code{&} becomes \verb{_and_}, \code{/} becomes \verb{_or_}.
\item Space, \code{-}, \verb{,}, \code{(} and \verb{)} each become \verb{_}.
\item Runs of \verb{_} collapse to one; leading and trailing \verb{_} are stripped.
}
}
\examples{
normalize_category_value("Non-Hispanic")
normalize_category_value("l&d")
normalize_category_value("pulmonary vasodilators (IV)")
normalize_category_value(c("DNR/DNI", NA))
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_query.R
\name{lookup_extremal_values_in_long_table}
\alias{lookup_extremal_values_in_long_table}
\title{Look up extremal values in a long CLIF table}
\usage{
lookup_extremal_values_in_long_table(
  ids_w_dttm,
  query_dict,
  table_name,
  data = NULL,
  config_path = NULL,
  data_directory = NULL,
  filetype = NULL,
  timezone = NULL
)
}
\arguments{
\item{ids_w_dttm}{A data frame with columns \code{hospitalization_id},
\code{start_dttm} and \code{end_dttm}.}

\item{query_dict}{Named list mapping category values to the extremal values
wanted, e.g. \code{list(weight_kg = c("latest", "max"), spo2 = c("max", "min"))}.
Valid extremal values are \code{"min"}, \code{"max"} and \code{"latest"}.}

\item{table_name}{One of \code{"vitals"}, \code{"labs"},
\code{"medication_admin_continuous"} or \code{"patient_assessments"}.}

\item{data}{Optional pre-loaded long table. When \code{NULL} the table is loaded
from the resolved configuration, filtered to the hospitalizations in
\code{ids_w_dttm}.}

\item{config_path}{Optional path to a configuration file, used only when
\code{data} is \code{NULL}.}

\item{data_directory, filetype, timezone}{Optional direct configuration
parameters, used only when \code{data} is \code{NULL}. See \code{\link[=get_config_or_params]{get_config_or_params()}}.}
}
\value{
A tibble keyed by \code{hospitalization_id}, \code{start_dttm} and \code{end_dttm}
with one additional column per requested \code{category_extremal} combination,
e.g. \code{spo2_max}, \code{weight_kg_latest}.
}
\description{
For each \verb{(hospitalization_id, start_dttm, end_dttm)} window in \code{ids_w_dttm},
finds the minimum, maximum and/or most recent value of each requested
category, and returns them pivoted into one column per
\code{category_extremal} combination.
}
\details{
Only measurements recorded inside the window (inclusive at both ends) with a
non-missing value are considered. Windows with no matching measurement do not
appear in the result, matching clifpy.
}
\examples{
\dontrun{
windows <- data.frame(
  hospitalization_id = "H001",
  start_dttm = as.POSIXct("2023-01-01", tz = "UTC"),
  end_dttm = as.POSIXct("2023-01-02", tz = "UTC")
)
lookup_extremal_values_in_long_table(
  windows,
  list(spo2 = c("min", "max"), weight_kg = "latest"),
  "vitals",
  data = vitals_table$df
)
}
}

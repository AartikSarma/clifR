% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_comorbidity.R
\name{calculate_elix}
\alias{calculate_elix}
\title{Calculate Elixhauser Comorbidity Index for hospitalizations}
\usage{
calculate_elix(hospital_diagnosis, hierarchy = TRUE)
}
\arguments{
\item{hospital_diagnosis}{A \code{HospitalDiagnosis} table object or a data frame
with columns \code{hospitalization_id}, \code{diagnosis_code} and
\code{diagnosis_code_format}.}

\item{hierarchy}{Logical, default \code{TRUE}. Apply the assign0 hierarchy logic
so that when both mild and severe forms of a condition are present, only
the severe form is counted.}
}
\value{
A tibble with one row per \code{hospitalization_id}, one binary (0/1)
integer column per Elixhauser condition (31 conditions, in the order
defined by the YAML configuration), and an integer \code{elix_score} column
with the van Walraven weighted sum.
}
\description{
Processes hospital diagnosis data to calculate Elixhauser scores using the
Quan (2011) adaptation for ICD-10-CM codes with van Walraven weights. Only
rows with \code{diagnosis_code_format == "ICD10CM"} (case-insensitive)
contribute; other code formats are discarded. Condition definitions and
weights are read from the packaged \code{comorbidity/elixhauser.yaml}, identical
to clifpy's.
}
\examples{
\dontrun{
hospital_diagnosis <- load_data("hospital_diagnosis", "data/clif", "parquet")
elixhauser_scores <- calculate_elix(hospital_diagnosis)
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_sofa_direct.R
\name{compute_sofa_direct}
\alias{compute_sofa_direct}
\title{Compute SOFA scores directly from raw CLIF tables}
\usage{
compute_sofa_direct(
  data_directory,
  cohort_df,
  filetype = "parquet",
  id_name = "hospitalization_id",
  extremal_type = "worst",
  fill_na_scores_with_zero = TRUE,
  remove_outliers = TRUE,
  timezone = NULL
)
}
\arguments{
\item{data_directory}{Directory containing the CLIF data files.}

\item{cohort_df}{Data frame with \code{hospitalization_id}, \code{start_dttm} and
\code{end_dttm}, plus any additional ID columns (e.g. \code{encounter_block}).}

\item{filetype}{\code{"parquet"} (default) or \code{"csv"}.}

\item{id_name}{Column to group scores by; defaults to \code{"hospitalization_id"}.}

\item{extremal_type}{\code{"worst"} (default); the only supported aggregation.}

\item{fill_na_scores_with_zero}{If \code{TRUE} (default), missing component scores
become 0.}

\item{remove_outliers}{If \code{TRUE} (default), drop physiologically implausible
\code{po2_arterial}, \code{fio2_set} and \code{spo2} values before aggregating.}

\item{timezone}{Optional Olson timezone for the loaded timestamps.}
}
\value{
A tibble with one row per ID: \code{sofa_cv_97}, \code{sofa_coag}, \code{sofa_liver},
\code{sofa_resp}, \code{sofa_cns}, \code{sofa_renal}, \code{sofa_total}, and the intermediate
worst values used to derive them.
}
\description{
Port of \code{clifpy.utils.sofa_polars.compute_sofa_polars}. Loads labs, vitals,
patient assessments, respiratory support and continuous medications from
\code{data_directory}, restricts them to each cohort member's observation window,
converts vasoactive doses to mcg/kg/min, and reduces everything to one
worst-value row per ID before scoring.
}
\details{
The respiratory component uses \emph{concurrent} P/F ratios: each arterial PO2 is
paired with the FiO2 in effect within the preceding four hours, and the lowest
resulting ratio (with its device) is used.
}
\examples{
\dontrun{
cohort <- data.frame(
  hospitalization_id = c("H1", "H2"),
  start_dttm = as.POSIXct(c("2024-01-01", "2024-01-02"), tz = "UTC"),
  end_dttm = as.POSIXct(c("2024-01-05", "2024-01-06"), tz = "UTC")
)
sofa_scores <- compute_sofa_direct("/path/to/clif", cohort, timezone = "UTC")
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_logging_config.R
\name{clif-logging}
\alias{clif-logging}
\title{Centralized logging configuration}
\description{
Port of \code{clifpy/utils/logging_config.py}. Provides a unified logging setup
that writes:
}
\details{
\itemize{
\item a main log file capturing everything at the configured level and above,
\item an error log file capturing warnings and errors only,
\item console output, so the user-facing experience is unchanged.
}

Log files live in \verb{<output_directory>/logs/}. clifpy names them
\code{clifpy_all.log} and \code{clifpy_errors.log}; clifR uses \code{clifr_all.log} and
\code{clifr_errors.log} so the two implementations can share an output directory
without interleaving.

The implementation is deliberately dependency-free beyond \code{cli}: R has no
equivalent of Python's \code{logging} hierarchy, so a small package-level state
object plus a logger closure covers the same ground.
}

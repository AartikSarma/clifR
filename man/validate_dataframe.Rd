% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_validator.R
\name{validate_dataframe}
\alias{validate_dataframe}
\title{Validate a data frame against a schema}
\usage{
validate_dataframe(
  df,
  schema,
  table_name = NULL,
  plausibility_thresholds = NULL
)
}
\arguments{
\item{df}{Data to validate.}

\item{schema}{Parsed table schema.}

\item{table_name}{Optional table name; taken from the schema when omitted.}

\item{plausibility_thresholds}{Optional named list of per-check threshold overrides.}
}
\value{
A list of error records, each a named list with \code{type},
\code{description}, \code{details}, \code{category} (\code{"schema"} or \code{"data_quality"}) and,
for warnings, \code{severity}.
}
\description{
Runs the conformance, completeness and plausibility suites and flattens the
findings into a list of error records. This is the interface \link{BaseTable}'s
\code{validate()} consumes, and the one clifpy exposes for CLIF-TableOne.
}
\examples{
\dontrun{
validate_dataframe(patient_data, load_schema("patient", "3.0"), "patient")
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_mdro_flags.R
\name{calculate_mdro_flags}
\alias{calculate_mdro_flags}
\title{Calculate MDRO flags for an organism}
\usage{
calculate_mdro_flags(
  culture,
  susceptibility,
  organism_name,
  cohort = NULL,
  hospitalization_ids = NULL,
  config_path = NULL
)
}
\arguments{
\item{culture}{A \code{MicrobiologyCulture} table object (or data frame) with
\code{organism_id}, \code{hospitalization_id} and \code{organism_category} columns.}

\item{susceptibility}{A \code{MicrobiologySusceptibility} table object (or data
frame) with \code{organism_id}, \code{antimicrobial_category} and
\code{susceptibility_category} columns.}

\item{organism_name}{Organism to calculate flags for; must match an
\code{organism_category} value and a key in the configuration, e.g.
\code{"pseudomonas_aeruginosa"}.}

\item{cohort}{Optional data frame with \code{hospitalization_id}, \code{start_dttm} and
\code{end_dttm}. When supplied, culture results outside each window are dropped.}

\item{hospitalization_ids}{Optional character vector restricting the analysis
to specific hospitalizations.}

\item{config_path}{Optional path to an \code{mdro.yaml} file. Defaults to the copy
bundled with clifR.}
}
\value{
A wide tibble with one row per \verb{(hospitalization_id, organism_id)}:
\itemize{
\item \code{hospitalization_id}, \code{organism_id} — identifiers.
\item \verb{<antimicrobial>_agent} — the susceptibility result for each agent, or
\code{"not_tested"}.
\item \verb{<group>_group} — \code{1} when any agent in the group is resistant, else \code{0}.
\item the resistance flag columns named by the configuration (\code{MDR}, \code{XDR},
\code{PDR}, \code{DTR} for \emph{P. aeruginosa}), each \code{0} or \code{1}.
}

Columns are ordered identifiers, then agents, then groups, then flags, each
block sorted by name.
}
\description{
Analyses antimicrobial susceptibility results to determine whether each
organism culture meets the multi-drug resistance criteria defined for that
organism in \code{mdro.yaml}.
}
\examples{
\dontrun{
culture <- MicrobiologyCulture$new(data_directory = "./data", filetype = "parquet")
susceptibility <- MicrobiologySusceptibility$new(
  data_directory = "./data", filetype = "parquet"
)
mdro_flags <- calculate_mdro_flags(
  culture = culture,
  susceptibility = susceptibility,
  organism_name = "pseudomonas_aeruginosa"
)
}
}

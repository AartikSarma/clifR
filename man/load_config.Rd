% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_config.R
\name{load_config}
\alias{load_config}
\title{Load a CLIF configuration file}
\usage{
load_config(config_path = NULL)
}
\arguments{
\item{config_path}{Path to a \code{.json}, \code{.yaml} or \code{.yml} configuration file.
When \code{NULL}, auto-detects one in the working directory.}
}
\value{
A named list of configuration values. Always contains
\code{data_directory}, \code{filetype} and \code{timezone}; may also contain
\code{output_directory}, \code{clif_version}, \code{site} and any other keys the file
defines.
}
\description{
Reads a JSON or YAML configuration file and validates that it defines the
fields CLIF table loading requires. When \code{config_path} is \code{NULL} the working
directory is searched for \code{config.json}, \code{config.yaml} and \code{config.yml}, in
that order.
}
\details{
A YAML \code{tables_path} key is mapped to \code{data_directory}, matching clifpy.
}
\examples{
\dontrun{
configuration <- load_config("config.yaml")
configuration$data_directory
}
}

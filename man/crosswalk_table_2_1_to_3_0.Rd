% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_crosswalk.R
\name{crosswalk_table_2_1_to_3_0}
\alias{crosswalk_table_2_1_to_3_0}
\title{Convert a table's standardized column values from CLIF 2.1 to 3.0}
\usage{
crosswalk_table_2_1_to_3_0(df, table_name, crosswalk_path = NULL)
}
\arguments{
\item{df}{A data frame of a site's table data in CLIF 2.1 format.}

\item{table_name}{snake_case CLIF table name, e.g. \code{"respiratory_support"}.}

\item{crosswalk_path}{Optional override path to the crosswalk resource.}
}
\value{
A named list with two elements:
\itemize{
\item \code{data} — a tibble copy of \code{df} with converted values.
\item \code{report} — a named list with \code{table}, \code{from_version}, \code{to_version},
\code{columns} (per-column \code{n_values_converted} / \code{ambiguous} / \code{unresolved})
and \code{is_complete}.
}
}
\description{
Transforms values in \verb{*_category}/\verb{*_group}/\verb{*_type} columns to their CLIF 3.0
form. Each value is mapped via the curated rename map, else by
\code{\link[=normalize_category_value]{normalize_category_value()}}. Values flagged as one-to-many \code{unresolved}, such
as \code{albumin}, are left unchanged, as are values that do not resolve to a valid
3.0 permissible value. All such cases appear in the report.
}
\details{
Column header names are not changed, and the input is never mutated.
}
\examples{
respiratory_support_21 <- data.frame(
  device_category = c("High Flow NC", "IMV", "Nasal Cannula")
)
converted <- crosswalk_table_2_1_to_3_0(respiratory_support_21, "respiratory_support")
converted$data$device_category
}

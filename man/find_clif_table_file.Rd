% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_io.R
\name{find_clif_table_file}
\alias{find_clif_table_file}
\title{Locate a CLIF table's data file}
\usage{
find_clif_table_file(data_directory, table_name, filetype)
}
\arguments{
\item{data_directory}{Character. Path to directory containing data files.}

\item{table_name}{Character. Name of the CLIF table (e.g. "patient").}

\item{filetype}{Character. File type: "csv" or "parquet".}
}
\value{
Character path to the located file, or \code{NA_character_} if no
matching file is found.
}
\description{
Searches a data directory for the file belonging to a given CLIF table.
CLIF data directories are not guaranteed to use a single naming
convention: some sites name files exactly after the table
(e.g. \code{patient.parquet}), while many CLIF consortium datasets use a
\code{clif_} prefix (e.g. \code{clif_patient.parquet}). This helper checks both
conventions, falling back to a case-insensitive match, so table lookup
does not depend on a single hardcoded filename pattern.
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_io.R
\name{clif_table_file_path}
\alias{clif_table_file_path}
\title{Build the path to a CLIF table's data file}
\usage{
clif_table_file_path(data_directory, table_name, filetype)
}
\arguments{
\item{data_directory}{Character. Path to directory containing data files.}

\item{table_name}{Character. Name of the CLIF table (e.g. "patient").}

\item{filetype}{Character. File type: "csv" or "parquet".}
}
\value{
Character path to the table's expected file.
}
\description{
The single source of truth for where a CLIF table's file lives. CLIF data
directories use the \code{clif_} prefix convention, so the file for table
\code{patient} in a parquet directory is \code{clif_patient.parquet} — there is no
fallback to a bare \code{patient.parquet} and no case-insensitive search. One
rule, applied everywhere, so a table cannot be found by one caller and
missed by another.

This only builds the path; it does not check that the file exists. Callers
test with \code{\link[=file.exists]{file.exists()}} and raise their own error, which lets a missing
optional table be skipped while a missing requested table aborts.
}
\examples{
clif_table_file_path("data/clif", "patient", "parquet")

}

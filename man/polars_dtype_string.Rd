% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_validator.R
\name{polars_dtype_string}
\alias{polars_dtype_string}
\title{Describe an R column the way clifpy's Polars backend does}
\usage{
polars_dtype_string(column_values, column_name = "")
}
\arguments{
\item{column_values}{A vector.}

\item{column_name}{The column's name, used for the timezone heuristic.}
}
\value{
A single dtype string.
}
\description{
clifpy renders the observed dtype into its error messages using Polars'
\code{repr}, e.g. \code{Datetime(time_unit='us', time_zone=None)}. This function
produces the equivalent string for an R vector so error descriptions match
verbatim.
}
\section{Timezone approximation}{

R stores a single \code{tzone} attribute per POSIXct vector and the DuckDB reader
labels every timestamp \code{UTC}, so a naive source column is indistinguishable
from a UTC-aware one once loaded. CLIF stores every timezone-aware timestamp
in a \verb{*_dttm} column, so a POSIXct column whose name lacks \code{dttm} is
reported as timezone-naive, matching what clifpy sees.
}

\keyword{internal}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_config.R
\name{get_config_or_params}
\alias{get_config_or_params}
\title{Resolve configuration from parameters or a configuration file}
\usage{
get_config_or_params(
  config_path = NULL,
  data_directory = NULL,
  filetype = NULL,
  timezone = NULL,
  output_directory = NULL
)
}
\arguments{
\item{config_path}{Optional path to a JSON or YAML configuration file.}

\item{data_directory}{Optional directory containing the CLIF data files.}

\item{filetype}{Optional file type, \code{"csv"} or \code{"parquet"}.}

\item{timezone}{Optional Olson timezone name.}

\item{output_directory}{Optional directory for logs and outputs.}
}
\value{
A named list with at least \code{data_directory}, \code{filetype} and
\code{timezone}, plus \code{output_directory} and any other configured keys.
}
\description{
Port of clifpy's \code{get_config_or_params}. Priority, highest first:
}
\details{
\enumerate{
\item If \code{data_directory}, \code{filetype} and \code{timezone} are all supplied, they are
used directly and no configuration file is read.
\item Otherwise a configuration file is loaded — from \code{config_path} when given,
else auto-detected in the working directory.
\item Any explicitly supplied parameter overrides the corresponding file value.
}

Extra keys in the configuration file, notably \code{clif_version}, are passed
through unchanged so callers such as \code{\link[=clif_table_from_file]{clif_table_from_file()}} can use them.
}
\examples{
resolved <- get_config_or_params(
  data_directory = tempdir(),
  filetype = "parquet",
  timezone = "UTC"
)
resolved$filetype
}

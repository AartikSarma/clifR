% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_report_generator.R
\name{generate_combined_validation_html}
\alias{generate_combined_validation_html}
\alias{generate_combined_validation_pdf}
\title{Generate a combined HTML DQA overview across tables}
\usage{
generate_combined_validation_html(
  table_results,
  output_path,
  table_names,
  site_name = NULL,
  feedback_map = NULL,
  display_names = NULL
)

generate_combined_validation_pdf(
  table_results,
  output_path,
  table_names,
  site_name = NULL,
  feedback_map = NULL,
  display_names = NULL
)
}
\arguments{
\item{table_results}{Named list mapping \verb{table_name -> parsed DQA result | NULL}.}

\item{output_path}{Path for the HTML file.}

\item{table_names}{Ordered vector of table names (controls row order).}

\item{site_name}{Optional site/hospital label.}

\item{feedback_map}{Optional named list mapping \verb{table_name -> feedback list}.}

\item{display_names}{Optional \code{table_name -> label} mapping; falls back to
\code{TABLE_DISPLAY_NAMES}.}
}
\value{
\code{output_path}, invisibly.
}
\description{
clifR's portable stand-in for clifpy's \code{generate_combined_validation_pdf}.
Renders the "DQA Overview" table — one row per table with Conformance,
Completeness, Plausibility and Overall columns, plus a totals row — as a
self-contained HTML document.

\code{generate_combined_validation_pdf()} is a thin alias for clifpy call-site
compatibility that forwards to \code{\link[=generate_combined_validation_html]{generate_combined_validation_html()}} and
announces the HTML substitution.
}
\examples{
\dontrun{
loaded <- collect_table_results("output/validation", c("patient", "adt"))
generate_combined_validation_html(loaded$results, tempfile(fileext = ".html"),
                                  c("patient", "adt"))
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_outlier_handler.R
\name{get_outlier_summary}
\alias{get_outlier_summary}
\title{Summarise potential outliers without modifying the data}
\usage{
get_outlier_summary(table_obj, outlier_config_path = NULL)
}
\arguments{
\item{table_obj}{A clifR table object with \verb{$df}, \verb{$table_name} and \verb{$schema}.}

\item{outlier_config_path}{Optional path to a custom outlier configuration
YAML. When \code{NULL}, the packaged CLIF standard configuration is used.}
}
\value{
A named list with elements \code{table_name}, \code{total_rows}, \code{config_source}
(\code{"CLIF standard"} or \code{"Custom"}) and \code{outliers} — a list of records, one per
range that has at least one violation, each with \code{column}, \code{min_expected},
\code{max_expected}, \code{total_values}, \code{below_min_count}, \code{above_max_count},
\code{total_outliers} and the corresponding percentages. Category-scoped records
also carry \code{category} (and \code{primary_category} / \code{secondary_category} for
medication ranges). When the table cannot be summarised, a list with a single
\code{status} element is returned instead.
}
\description{
Non-mutating counterpart to \code{\link[=apply_outlier_handling]{apply_outlier_handling()}}. Reports, per
configured range, how many non-missing values fall below the minimum and above
the maximum.
}
\examples{
\dontrun{
vitals <- clif_table_from_file("vitals", "data/clif", "parquet", clif_version = "3.0")
summary_result <- get_outlier_summary(vitals)
length(summary_result$outliers)
}
}

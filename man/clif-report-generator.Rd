% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/util_report_generator.R
\name{clif-report-generator}
\alias{clif-report-generator}
\title{DQA report generation}
\description{
Port of \code{clifpy/utils/report_generator.py} (clifpy 0.5.0). Turns the
\code{validation_data} a \code{\link[=run_full_dqa]{run_full_dqa()}} call produces into human-readable DQA
reports.
}
\details{
clifpy renders a PDF (via reportlab), a plain-text report, and a consolidated
CSV. reportlab has no R equivalent and PDF byte-parity is neither achievable
nor useful, so the two PDF entry points here emit a \strong{self-contained HTML}
report with the same content and section structure instead
(\code{\link[=generate_validation_html]{generate_validation_html()}}, \code{\link[=generate_combined_validation_html]{generate_combined_validation_html()}}); thin
\verb{*_pdf()} aliases forward to them and announce the substitution. Every other
output — the text report, the consolidated CSV, and the
\code{\link[=collect_dqa_issues]{collect_dqa_issues()}} / \code{\link[=compute_table_stats]{compute_table_stats()}} structures — is ported
faithfully and matches clifpy byte-for-byte (modulo the run timestamp).

All report inputs are the \code{validation_data} list returned by \code{\link[=run_full_dqa]{run_full_dqa()}}
(optionally augmented by the caller with \code{table_stats} and \code{total_rows}).
Validation itself is never re-implemented here.
}
